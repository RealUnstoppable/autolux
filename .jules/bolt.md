## $(date +%Y-%m-%d) - Preload and Sizing Optimization
**Learning:** Adding `width` and `height` to lazy loaded images in this vanilla JS app prevents Layout Shifts (CLS), and preloading the LCP image improves perceived load time.
**Action:** Always check for missing image dimensions and hero preloading opportunities in static HTML pages to improve web vitals.
